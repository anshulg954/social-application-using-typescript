import * as JobScheduler from "node-schedule";
export class Email{
  /* CRON JOB 
    static runEmailJob(){
        this.sendEmailJob();
    }
    static sendEmailJob(){
        JobScheduler.scheduleJob('send Email JOB','01 * * * * *',()=>{
            console.log('Email Job scheduled!');

        })
    }
    */
}