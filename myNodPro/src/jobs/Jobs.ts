import { Database } from "./Database";
import { Email } from "./Email";

export class Jobs{
    /* CRON JOB
    static runRequiredJobs(){
        Database.runDatabaseJob();
        Email.runEmailJob();
        
    }
    */
}